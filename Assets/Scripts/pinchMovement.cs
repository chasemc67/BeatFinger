namespace BeatFinger.Assets.Scripts
{
    public class pinchMovement
    {
        void Update()
    {
        //check for middle finger pinch on the left hand, and make che cube red in this case
        // if (rightHand.GetFingerIsPinching(OVRHand.HandFinger.Middle)) {
        //     m_renderer.material.color = Color.red;
        // } else if (m_renderer.material.color == Color.red) {
        //     m_renderer.material.color = Color.white;
        // }
    }
    }
}